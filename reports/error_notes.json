{
  "summary": [
    "**No test distress case was missed, and none reached the skip band.** 0 of 52 distress rows skipped the LLM, and the lowest worst-segment score among them was well clear of `low`. The skip band was fitted so that no *probe* note could skip; that it also holds for 52 held-out distress rows it never saw is the part this look actually tested.",
    "**No note reached support, which is what the closed band means.** The previous artifact sent 27 of 139 non-distress rows straight to the support message, and diverted 8 of `bloom-langgraph`'s encouragement cases. This one sends none, because `high` is 1.000. That failure mode is gone by construction rather than by improvement.",
    "**The cost is the golden guarantee.** The previous artifact routed all 10 golden distress cases to support *without the LLM* — a gate that held whatever Haiku said. All 10 now escalate instead, scoring 0.904 to 1.000. They are ranked correctly and would clear any cutoff below 0.9; the constraint simply forbids one. So their recall is no longer guaranteed here — it depends on a call this evaluation does not make.",
    "**What the cascade is now.** On the 147 rows with votes it matches the LLM alone exactly: recall 1.000 against 1.000, 2.3 expected false alarms against 2.3. It differs in one respect only — it makes 86% as many LLM calls. This is a cost-reduction component whose safety obligation is to change nothing else, and on this test set it changed nothing else.",
    "**What this look cannot tell you.** 52 distress rows put a 95% lower bound of 0.931 on skip-band safety, not a guarantee; the golden rows have no recorded votes, so the gate is reported as bounds; and the probe notes that set `low` are excluded from being evidence about it by the same fit that used them.",
    "**This analysis replaces the one written for the superseded artifact.** That version diagnosed 27 false alarms from the support band, and before that attributed seven errors to matched pairs split across train and test and called one injection successful — a claim the nearest-neighbour explanations did not support, corrected at the time. None of those errors exist under this artifact; the diagnoses are kept in the ledger's history, not here, because they describe routes it no longer takes."
  ],
  "errors": {
    "ds-1": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.915 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-2": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.982 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-3": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.981 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-4": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.904 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-5": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.999 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-6": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 1.000 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-7": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.980 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-8": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.993 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-9": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.926 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    },
    "ds-10": {
      "cause": "Escalated, not routed: the release-gate constraint closed the support band",
      "note": "Scores 0.992 — ranked as distress, and would reach support under any cutoff below 0.9. It escalates because `high` is 1.000, so whether it is caught depends on claude-haiku-4-5, which this evaluation does not call."
    }
  }
}
