# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# venv / uv
.venv/

# Secrets: never commit a real key. Keep the template.
.env
.env.*
!.env.example

# tooling caches
.pytest_cache/
.ruff_cache/
.pyright/

# OS
.DS_Store

# Vendored embedding-model weights: fetched once at build time, never committed.
# Mirrors bloom-langgraph — the derived artifacts (splits.json, model.npz) ARE
# committed as the audit surface; the 90MB of borrowed weights are not.
models/


# Private working doc: the plan of record lives outside the repo.
BUILD_SPEC.md
